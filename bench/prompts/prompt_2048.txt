Create a detailed benchmark methodology for on-device Qwen3.5-9B inference on Android. The methodology must define stock MNN and custom MNN-based library runs, require identical Hugging Face revision, tokenizer, prompt, prompt token count, quantization bitwidth, group size, context length, max_new_tokens, greedy decoding settings, warmup, thread count, affinity policy, and backend class. It must define prefill tokens per second, decode tokens per second, first-token latency, total generation time excluding load, memory RSS/PSS, native heap, thermal state before and after, battery state, CPU/GPU/NPU probes, AWS Device Farm identifiers, and raw artifact retention. It must explain why custom W2 results cannot be compared against MNN W4 as the main speedup. It must require exact Samsung Galaxy S26 Ultra availability through AWS Device Farm APIs and mark final validation blocked if the device is unavailable or credentials are missing.

