#include "../../kernels.hpp"

namespace xq {
namespace kernels {

void {{FUNC_NAME}}(const QuantizedMatrix& matrix, const float* x, float* y) {
    gemvLowBitReference(matrix, x, y);
}

}  // namespace kernels
}  // namespace xq

